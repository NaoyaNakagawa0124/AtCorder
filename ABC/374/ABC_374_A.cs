using System;
class Program
{
    static int Main()
    {
        // 文字列の読み込み
        string S = Console.ReadLine();
        string T = Console.ReadLine();

        if(readLine[readLine.Length - 3] == 's')
        {
            if(readLine[readLine.Length - 2] == 'a')
            {
                if(readLine[readLine.Length - 1] == 'n')
                {
                    Console.WriteLine("Yes");
                    return 0;
                }
            }
        }
        Console.WriteLine("No");
        return 0;
    }
}